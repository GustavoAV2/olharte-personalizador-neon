export type User = {
  id: string;
  name: string;
  username: string;
  password: string;
};

export type Font = {
  id: string;
  name: string;
  weight: number;
}

export type Color = {
  id: string;
  hexcode: string;
}

export type Image = {
  id: string;
  path: string;
  filename: string;
}
